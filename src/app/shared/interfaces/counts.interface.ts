export interface ICounts {
    permission: {
        permissions: {
        name: string,
        keys: string[],
        value: number[],
    },
    total: 0,
    };
    roles: number;
    users: number;
}
